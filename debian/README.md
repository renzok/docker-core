Base docker image for allmost all other docker images in this
repository. It is itself based on debian:jessie

Main features:

  * has user: mate
  * and user has group: bootswain
  * less, curl installed
  * UTF-8 locale
